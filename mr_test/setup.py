from setuptools import setup

setup(
    name="mr-test",
    version="1.0.0",
    description="A sample Python package",
    author="chatgpt",
    packages=["hello"],
    install_requires=["numpy>=1.15.0"],
)
